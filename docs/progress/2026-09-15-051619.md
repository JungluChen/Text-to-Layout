# Numerical and reference-validation audit

**Document class: MANUAL_DOCUMENTATION.** Run started 2026-09-15 05:16:19
Asia/Taipei (`2026-09-14T21:16:19.621Z`), resuming the usage-blocked first
scheduled attempt. Selected backlog item: **1, numerical/reference audit**.
Initial local `main`: `8ab951a998ff6a30de683c24e7a8d73382dc0cdc`;
fetched origin/main: `32c2ae4a3130ffd711c1f60c3daf7f8d614bfe9f`.
Host: macOS 26.6.2, arm64, Python 3.12.14. Per-command dependency versions,
source hashes, environment, timestamps, duration and output hashes are retained
in [the evidence manifest](evidence/2026-09-15-051619/manifest.json).

## Reviewed and corrected behavior

Reviewed the existing unpublished commits `f03918c`, `52ac988`, `c613767` and
uncommitted typed-goal/reference-mask work. The initial setup's `0681852`
pointer is absent from current local history; do not infer its contents or
restore it speculatively. Initial process inspection found no active project
tests or solvers to resume. Remote documentation CI/test runs for `32c2ae4`
were successful before publication.

- Integrated strict electrical goals, CLI `textlayout design`, demo and
  `POST /layout/from-goal`: explicit physical quantities, process minima,
  footprint checks and bounded feasibility rejection through the shared graph.
- Found a real conflicting-verdict defect: the graph wrote `verification.json`
  and `report.md` before the final requirements target check. A 6 GHz quarter-wave
  request with diagnostic tolerance `1e-12%` returned failure while the general
  artifact still said pass. Both pre-fix runs reproduced the same
  `2.2112920383676737e-07%` error. The final target verdict is now written to
  all consumer reports. Both original reruns pass consistency checks; CLI
  regression cases cover passing and failing targets without changing tolerance.
- The public-claim audit reproducibly flagged an OCI state-order violation:
  Docker is unavailable on this Mac, but a historical image evidence file
  incorrectly passed a later integration gate. That gate now requires both
  available runtime and retained image evidence. The OCI capability remains
  **IMPLEMENTED**, blocked at runtime; evidence hashes are preserved. The
  evaluator's rejection of out-of-order evidence remains tested and unchanged.
- Kept source-linked Apple UI/UX requirements, function catalog and guidebook
  aligned with the typed-goal increment. Added a verified CLI tutorial and AI
  prompt. GUI screenshots, desktop/browser delivery and modern MCP/plugin
  parity remain pending; they are not certified by CLI tests.

## Scientific findings and limits

Two fresh `run_benchmark_validation.py --with-fasthenry` runs agree on all
analytical values/verdicts, all extracted values/target comparisons, family
summaries and paper-model results. Comparison fields and exclusions are in
[repeat-comparison.json](evidence/2026-09-15-051619/repeat-comparison.json).
Runtime, temporary output paths and path-dependent artifact hashes are retained
but are not expected to be identical numerical results.

| Population | Repeated result | What it establishes |
| --- | --- | --- |
| Local engineering grid | 64/64; four families of 16, target tolerance 0.1% | Analytical sizing and geometry checks, not independent full-wave validation. |
| Native FastHenry spirals | 16/16 executed and pass 5% target tolerance | Normal-metal quasi-static extraction. FastHenry 3.0.1 executable SHA-256 `007a93526fa931ecea0e86fe37eb43a9b5b88523325c644072fec6392563c3ce`. |
| Mohan Table IV supported subset | 29/29 printed model-error rows within 0.5 percentage points | Analytical model reproduction; RMS measurement error 8.9369% versus paper 8.9787%, maximum printed-error deviation 0.2146 pp, worst measurement APE 19.749%. |
| SQuADDS WM1 original mask | 4/4 variants pass exact per-layer XOR, labels, bounding box and DBU | Mask preservation through gdsfactory and independent KLayout readback. |

Fresh solver inputs, manifests, stdout/stderr and impedance matrices are retained
alongside both result matrices. FastHenry results do not establish mesh convergence,
superconducting kinetic inductance or matching paper-device parity. The WM1
comparison retains six published measurement records but reports electrical
reproduction **NOT_EVALUATED**: top-variant mapping, process/ports and converged
electrical extraction are unresolved. Primary source revisions, source hashes,
assumptions and tolerance rationale remain in `BENCHMARK_SPEC.md` and benchmark
JSON. No unlicensed source GDS is redistributed and no upstream notebook ran.

Palace evidence-consistency checks pass while the scientific benchmark still
has seven blockers: adaptive refinement, elementwise indicators, last-level
frequency change, global AMR error, domain sweeps, domain sensitivity and an
independent reference. This run does not restore or certify Palace execution.

## Verification and reproduction

Working directory for commands: `/Users/ludko/Desktop/text2layout/Text-to-Layout`.
The retained `commands/*.json` records each exact invocation, exit code and log
hash; paired `.log.txt` files preserve full output. The reproduction scripts
are retained as text under `reproduce/`.

- Two baseline `uv run pytest` runs: **1,952 passed, 2 failed, 11 skipped**.
  The failures were sandbox restrictions on localhost binding and process
  inspection. Both exact tests passed with the required host access; no product
  change was made for the sandbox failures.
- After the target-verdict fix, full suites passed **1,956 tests, 11 skipped**.
- Adding four OCI regression cases exposed mistakes in the new test fixture
  (missing fake Git identity and treating a list of evidence records as a map).
  These failures were reproduced, the fixture corrected, and all four tests
  passed twice. Generated count assertions were stale after new JUnit evidence;
  `scripts/generate_project_status.py` refreshed them instead of hand edits.
- Final full-suite publication runs: **1,960 passed, 0 failed, 11 skipped**.
  Existing dependency/model warnings remain (518,005 warnings); no tests were
  disabled. Skips remain skips, not platform or solver certification.
- `uv run ruff check .`, `uv run mypy` (182 source files) and `uv build` pass.
- Claim audit passes twice after the classification correction. Namespace,
  README claims, strict benchmark audit, canonical evidence, Palace AMR evidence,
  showcase drift, cross-artifact consistency, Palace retained-output evidence,
  plugin bundle and generated-status checks pass.
- Acceptance regeneration and default benchmark generation leave their tracked
  artifact directories unchanged. Default benchmark generation explicitly skips
  four current artifacts and leaves two planned cases TODO.
- The typed 50 ohm goal command passes with analytical target error about 0.002%
  and `SIMULATION_INPUT_PREPARED`; it does not claim solver execution.

Earlier AUDIT_LOG/README counts are historical observations. The current generated
status cites the final JUnit evidence. Large geometry/preview files remain in
ignored `out/daily/2026-09-15-051619/`; compact evidence is retained in Git.

## Publication and continuation

Verified implementation commits, recorded after creation:

- `d1061bce7827c55fe5083bf2d83adc37dab510a0`: typed electrical goals and consistent final reports.
- `5f114b2039cdb1f2f37056d44eb0e4426891cb56`: pinned reference-mask comparison and evidence.
- `fa6a31af51374bf2dc051061d261c527ce3a6268`: truthful OCI runtime/evidence classification.

These integrate the reviewed pre-existing commits `f03918c`, `52ac988` and
`c613767`. This documentation checkpoint is prepared after the implementation
commits; pushed-SHA CI results will be recorded after publication. Pending CI
must remain pending until checked.

During publication, `/usr/bin/git` began returning exit 69 with an Xcode license
prompt. No commit was created by those failed commands. The already-installed
`/Library/Developer/CommandLineTools/usr/bin/git` (2.50.1) works and was used
explicitly for the commits. No license was accepted and no global developer
directory or Git identity setting was changed. Use that Git path on continuation
if the system launcher remains blocked.

The saved automation was found at 05:15 during continuation and was updated through
the app to the user's requested **daily 01:00 Asia/Taipei**, ACTIVE, normal
notifications. Next regular occurrence after this run: **2026-09-16 01:00 Taipei**
(`2026-09-15 17:00 UTC`). The same conversation/model remains attached. Keep the
Mac powered on and Codex running. The first-attempt reporting gap is retained
in `2026-09-15-010350.md` rather than marked successful retroactively.

Next: inspect CI for the exact pushed SHA; resolve any failed platform/gate jobs
before marking backlog item 1 complete. Then investigate Palace run
[34824773767](https://github.com/JungluChen/Text-to-Layout/actions/runs/34824773767):
installation and smoke passed but the reduced benchmark reported
`SKIPPED_SOLVER_ABSENT`. Start with
`gh run view 34824773767 --repo JungluChen/Text-to-Layout --log-failed`, compare
the smoke and benchmark discovery/invocation environments, then reproduce the
smallest faithful case twice before editing. Reuse any active jobs/artifacts.
Docker integration, commercial comparisons, other-platform certification and
unimplemented UI/plugin/illustration milestones remain explicit pending work.

## Published result and final checkpoint

The implementation and evidence were pushed successfully to `origin/main` at
`99d8d06474b5c5e9e0dfd222c8fe1ba3f92d0a0b`. Remote `refs/heads/main` matched
that hash and the working tree was clean before this final documentation update.
Both exact-SHA workflows completed successfully on 2026-09-15:

- [CI 34928428006](https://github.com/JungluChen/Text-to-Layout/actions/runs/34928428006):
  all six Ubuntu/Windows/macOS × Python 3.11/3.12 jobs, quality gates and bundle sync passed.
- [Windows test 34928427980](https://github.com/JungluChen/Text-to-Layout/actions/runs/34928427980):
  compile, full pytest, lint and type check passed (job `104251355131`).

Full job/step records are retained in `ci-34928428006.json` and
`ci-34928427980.json` under this run's evidence directory. These are executed
package/CLI/API CI checks, not desktop-installer, hosted-service or real-solver
certification. Backlog item 1 is complete for the published audited work.
Any workflow for this subsequent documentation-only checkpoint must be inspected
separately; the successful results above apply specifically to `99d8d06`.

The user's pasted continuation instructions were read before publication, and
the final CI review resumed at approximately 12:20–12:27 Taipei. No unrelated
process was canceled and no additional solver job was started. The next daily
schedule remains 2026-09-16 01:00 Taipei.

For Palace item 2, downloaded the existing compact artifact `10342591878` and
retained selected installation, smoke and failed-preflight records with hashes
under `palace-existing-failure/`. The official smoke uses native executable
`/home/runner/.cache/textlayout-palace/spack-opt/linux-x86_64_v3/palace-0.17.0-cz3ebqc5tl6ybo3bu4vyrjdcqhdoo3g6/bin/palace`
with SHA-256 `ad43ec030f51435f32150a5c72cb324ca03083d2ffc2f91405bbf41c6bc2240f`.
The subsequent benchmark reports no executable found. Code inspection identifies
a diagnostic lead: `detect_palace_capability` uses `install.json` only when its
path starts with `wsl:`, leaving a native Spack path outside PATH undiscovered.
No Palace code was changed on this evidence alone. Next action is a faithful
native-install-record discovery reproduction twice, followed by a focused fix
and actual Linux solver-enabled verification. Original smoke success must not
be substituted for reduced-benchmark execution or convergence.

Final documentation checkpoint prepared: 2026-09-15T12:27:07+08:00 (Asia/Taipei).
