# Palace repeated mode rejection and diagnostic instrumentation

**Document class: MANUAL_DOCUMENTATION.** Run 2026-09-27 01:25–01:39 Asia/Taipei, macOS arm64. Selected Priority 0 accuracy slice and item 2. Main began at `229c7421e643edd352b5f51c90578c9ca2532b6a` locally, one commit ahead of the previously fetched origin; that already validated documentation commit was pushed to `origin/main` at the start of this run. The isolated Palace branch began clean at `f407c6330e2e36de1fc70024211669ede450ac3d`. There were no active local solver processes. The only Palace workflow at the start had completed failed; no existing solver job was duplicated.

## Reproduced scientific failure

[Palace run 36223218053](https://github.com/JungluChen/Text-to-Layout/actions/runs/36223218053), attempt 2, job `108363856056`, executed unchanged at `f407c63`. Installation and official smoke succeeded; bounded diagnostic step 15 again exited 1 with `SIMULATION_INVALID` and `ambiguous_mode_identity: physical target classification failed: TARGET_MODE_NOT_FOUND`. Both attempts used pinned Palace 0.17.0 binary SHA-256 `ad43ec030f51435f32150a5c72cb324ca03083d2ffc2f91405bbf41c6bc2240f` and Gmsh 4.15.2. The second artifact was downloaded with `gh run download 36223218053 --name palace-bounded-diagnostic-2` and retained under [`evidence/2026-09-27-013505/palace-attempt2/`](evidence/2026-09-27-013505/palace-attempt2/).

The two attempts have byte-identical `bounded_solved_states.json` (`2601a538e4a3bcf7234bb3f65746906ffa949eab3b63617585a1d0c84e18b342`), `mode_tracking.json` (`8d265d6bec31526116aee1d3874ab50bd80a4b2d638d2c9eb66a5d219e8e022f`), solver `eig.csv` (`bf1532d3e70acaedf69281af898fab95c4cbe5aa6e3a583e16d2967f703ec888`), regional energy table, and material overlap map. Both solved states have 106,614 then 114,276 elements, under the 200,000 guard. Mode 1 at 5.096192768261 GHz failed the quarter-wave profile gate; mode 2 at 5.691721374551 GHz failed endpoint, profile, localization and package-energy gates. No mode is accepted as the target. Mesh-generation runtime and measured memory varied modestly (peak RSS 1,973 versus 1,972 MiB); canonical evidence differed only by timestamp. These differences do not change the repeated scientific verdict. Full benchmark, convergence, domain sweeps, and reference comparison remain **skipped by the failed mode-identity prerequisite**. The candidate cannot be merged to main or presented as a valid simulated resonator result.

## Focused diagnostic change and verification

The branch now has diagnostic commit `e4f52cc4a8a4055ac10ddae2c2fbb5a5ee771b44`, pushed to `origin/codex/palace-native-discovery-20260915`. It retains each mode's classified signature, longitudinal electric/magnetic energy profiles, endpoint ratios, phase scores, and physical-gate reasons in `mode_tracking.json` even when target selection fails. It does not change geometry, materials, solver settings, physical thresholds, or the invalid verdict. A regression test checks that a rejected synthetic mode retains the measured profile and rejection reasons. The next real run will expose why the current field profile failed; this code alone does not repair or validate the physics.

`uv run --active --no-sync` used the existing Python 3.12 development environment with the isolated branch on `PYTHONPATH`. Full pytest passed **1,979 passed, 12 skipped** in 45.70 s. Focused Palace tests passed 26/26. Ruff, mypy, offline non-isolated wheel/sdist build, strict benchmark, README claim, namespace, generated-status, canonical-evidence, Palace, showcase and cross-artifact consistency gates passed as commands. The Palace gate still reported **seven scientific blockers** despite exit 0. The claim audit generator ran and wrote local-environment downgrades because its fresh-install evidence is absent in this isolated worktree; those four generated outputs were restored from branch HEAD rather than committed as a product change. No platform installation or new scientific accuracy claim was made.

The new exact-SHA [generic CI 36259679268](https://github.com/JungluChen/Text-to-Layout/actions/runs/36259679268) and [real Palace run 36259710684](https://github.com/JungluChen/Text-to-Layout/actions/runs/36259710684) were in progress when inspected. The real Palace workflow was dispatched once with `run_benchmark=true` after confirming no active Palace job. If this diagnostic attempt reproduces the mode rejection, preserve its artifact and rerun unchanged on the same SHA before a physical model/classifier edit. Generic CI is not Palace scientific acceptance.

Main's preceding documentation commit `229c742` was pushed this run; its exact-SHA [CI 36259097523](https://github.com/JungluChen/Text-to-Layout/actions/runs/36259097523) and [test 36259097531](https://github.com/JungluChen/Text-to-Layout/actions/runs/36259097531) both passed. The earlier evidence commit `cb549d2` also passed its exact-SHA CI/test. The source audit still records 0/100 fully accepted integrations. Windows/macOS/Linux installed apps, automatic updates, npm publication, PyPI publication and commercial comparisons remain pending their own evidence.

## Exact continuation

From `/Users/ludko/Desktop/text2layout/Text-to-Layout`, monitor existing Palace run `36259710684` and branch CI `36259679268`; do not start another solver while it is active:

```sh
gh run view 36259710684 --repo JungluChen/Text-to-Layout --json status,conclusion,jobs
```

When complete, download its `palace-bounded-diagnostic-1` artifact and inspect `mode_tracking.json.mode_diagnostics_by_iteration.iteration_00` for mode 1's electric/magnetic profile arrays, endpoint ratios, phase progression and correlation against the predeclared criteria. Compare with mode 2 and solver-owned fields. If the result differs from the previous two attempts, investigate environment/nondeterminism first. If it repeats, run the same candidate workflow unchanged once more; only then diagnose and edit the physical cause with a focused regression. Retain every artifact and exact SHA. Require a successful full reduced benchmark, convergence, independent public-reference error and applicable platform evidence before merging the Palace branch. `origin/main` must be fetched/reconciled before any eventual merge. This report's documentation commit and exact-SHA CI should be recorded in a later documentation update, avoiding a self-reference.
