# Checkpoint the Palace mesh and run a bounded diagnostic

**Document class: MANUAL_DOCUMENTATION.** Started 2026-09-25 22:20:28
Asia/Taipei on the local Apple Silicon Mac, Python 3.12.14. Backlog item 2:
scientifically valid Palace integration. Starting `main` was clean at
`64c9abeb489db79cad323609d3d83116e3f0fdbc` and matched `origin/main`.
Its exact-SHA CI and test workflows passed. No Palace Actions job or local
solver was active before this work. Repository agent policy, ordered backlog,
the prior progress report, current branch/CI state and workflow were read.

## Reproduction and diagnosis

The earlier candidate Palace run `36032456522` lost its runner inside the
reduced CPW benchmark. The official 0.17.0 smoke passed, but the job log API
returned `404 BlobNotFound` and no late artifacts survived; neither a cause
nor a reduced scientific result was established. Repeating that monolithic
workflow would not improve evidence retention.

The existing CLI advertises `--stage base_mesh` and `--resume`. A smallest
deterministic reproduction first produced a mesh-only stage using a fake mesh
writer and a non-executed placeholder capability, then resumed with the same
model/process/AMR inputs. It returned `SIMULATION_INVALID` because it demanded
already-completed base-AMR solver outputs. The same command reproduced twice
unchanged on starting candidate `be197b3`. The regression test failed twice
before the edit. This is a stage-control defect, not evidence that a real
Palace solve failed.

## Candidate implementation

The isolated branch `codex/palace-native-discovery-20260915` merged current
main, retaining its separately verified native-discovery fix. Four conflicts
were in stale troubleshooting/generated-status files; the newer main records
were selected, then candidate status was regenerated from its own full suite.

Candidate implementation **`2b883601bd96439309fe6414c813d0eefa909ec5`**
was pushed to that branch. Resume now accepts a completed base-mesh checkpoint
only after checking the stage record and both mesh/metrics hashes, with no AMR
configuration, solver logs, resource record, postprocessing folder or AMR stage
record present. A partial solve still follows the existing strict completed-AMR
validator; altered mesh evidence is rejected. The original completed-AMR path
and numerical models were not changed.

The workflow now uploads the verified base mesh and stage records before the
long solve. A separate one-rank/two-state coarse Palace profile has an 8 GiB
process-group RSS limit and 1,200-second solver limit and uploads compact
diagnostic outputs. It is operational evidence only: its different extents,
mode count and AMR budget cannot establish the full benchmark's convergence
or reference accuracy. On success, the original full benchmark continues with
two processes, mesh scale 4.0 and four AMR iterations, using `--resume` to
reuse the exact staged mesh. All final scientific promotion gates remain in
place. The early install/smoke artifact also remains.

## Verification and limits

| Check | Result |
| --- | --- |
| Pre-edit mesh-only resume, twice unchanged | `SIMULATION_INVALID`: completed AMR artifacts absent |
| New regression before edit, twice | Failed: solver path never reached |
| Mesh-only resume and partial/tampered evidence cases after edit | 3 passed, repeated unchanged |
| Full candidate pytest | **1,979 passed, 11 skipped**, 43.93 seconds |
| Ruff, mypy, build and all prescribed status/benchmark/evidence/claim gates | Passed |
| Workflow YAML step order and Bash syntax | Passed; full settings asserted unchanged |
| Real local Gmsh mesh | **Untested:** this Mac's current Python environment lacks Gmsh bindings |
| Real candidate Palace install, mesh, bounded/full solve and uploads | **Pending Linux CI** |

All local command logs, the reproduction script, Gmsh-unavailable report and
gate outputs are retained with hashes in the
[evidence manifest](evidence/2026-09-25-222028/manifest.json). The full JUnit
is tracked at `out/evidence/test_report.xml`; `PROJECT_STATUS.md` and
`out/evidence/project_status.json` were regenerated through
`scripts/generate_project_status.py`. A local fake mesh verifies control flow
and hash guards, not physical geometry or solver output. No Palace frequency,
convergence or commercial/reference comparison is claimed from this change.

## Git, CI and continuation

Candidate `2b88360` was pushed successfully. [Candidate CI `36148281544`](https://github.com/JungluChen/Text-to-Layout/actions/runs/36148281544)
passed all eight jobs, including both Python versions on Windows, macOS and
Ubuntu. A fresh Palace-job inventory found no active run. Exactly one
[`palace-integration` run `36148860525`](https://github.com/JungluChen/Text-to-Layout/actions/runs/36148860525)
was dispatched with `run_benchmark=true` on exact code SHA
`2b883601bd96439309fe6414c813d0eefa909ec5`; its
[`palace` job `108116750266`](https://github.com/JungluChen/Text-to-Layout/actions/runs/36148860525/job/108116750266)
was in progress at the first check. Inspect the early smoke, mesh and bounded
diagnostic artifacts before the full solve. If the bounded
diagnostic fails, retain its solver-owned output and diagnose it before any
numerical edit. If the full solve loses the runner, use the independently
uploaded checkpoints to locate the last completed stage; do not infer OOM
without measured counters or runner diagnostics.

The candidate remains isolated. Require real pinned installation, official
smoke, reduced solver execution, convergence, justified reference tolerance
and retained evidence before promoting discovery/resume to `main`. Later
desktop/web/MCP/guidebook roadmap items remain behind numerical correctness.
The daily automation remains active for 01:00 Asia/Taipei; the next regular
occurrence is 2026-09-26 01:00. While run `36148860525` is active, monitor
that run rather than dispatching a duplicate. Capture each artifact ID,
solver log and numerical result as it becomes available; if still active at
the next scheduled turn, continue monitoring without claiming completion.
