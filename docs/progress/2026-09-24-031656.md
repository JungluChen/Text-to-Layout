# Preserve Palace evidence before a runner interruption

**Document class: MANUAL_DOCUMENTATION.** Wake: 2026-09-24 03:16:56 Asia/Taipei.
Execution checkpoint prepared on September 24 after earlier delayed requests.
Backlog item 2, local Apple Silicon Mac, Python 3.12.14. Starting main:
`e121c01acd2048caccef34db376d5910fc4d59a0`, clean; fetched origin matched.
Repository instructions, diagnostic policy, backlog, prior reports, requirements,
design brief, function catalog and guidebook were reviewed. No active local solver
or remote Palace job was found before the new dispatch.

## Reproduced failure and limits

[Candidate attempt 2](https://github.com/JungluChen/Text-to-Layout/actions/runs/34932740535/attempts/2),
job `105933606196`, at unchanged `8705804045700d4b2bd87bd1d8e4c79839fd955d`,
failed again. Installation and official smoke passed. The reduced benchmark
started at 2026-09-19 18:20:35 UTC; the runner reported shutdown at 18:38:25 UTC.
The reduced step was cancelled, the final collectors/uploaders were skipped,
and the artifact API returned zero artifacts. Cleanup mentions an orphan mpirun;
that is not a retained parsed result or proof of convergence. Unlike attempt 1,
this log reports cancellation without an explicit exit 143. The shutdown cause
remains unknown: no OOM, manual cancellation or scientific defect is inferred.

Both unchanged attempts reproduce the evidence-retention gap: runner loss can
skip even `always()` cleanup. A pre-benchmark upload can preserve stages that
have completed before the loss; it cannot recover later benchmark outputs.

[Weekly main run 35580135563](https://github.com/JungluChen/Text-to-Layout/actions/runs/35580135563)
also failed, reporting `SKIPPED_SOLVER_ABSENT` after smoke on main's old discovery
path. It uploaded artifact `10632948308`. Its download encountered a TLS handshake
timeout; its contents were not inspected. This differs from candidate runner loss
because main still lacks the isolated native-path discovery fix.

September 22–23 GitHub reads were delayed; the observed request eventually failed
with a TCP read timeout. Fresh bounded API reads succeeded on September 24. Those
interruptions do not establish any new solver result or per-day code work.

## Focused change

Implementation `034fc15ac791439623a3edc8b591c0d3eacdd846` was committed and pushed
to `origin/main`. The workflow now uploads `palace-pre-benchmark-<attempt>` after
smoke succeeds, before the reduced benchmark. It includes the installation
record, toolchain JSON/logs, smoke input/result/command JSON, stdout/stderr,
solver-owned eigenmode CSVs, and starting memory/disk/process-limit information.
Only explicit diagnostic paths are uploaded, with 30-day retention. Binaries
and large field files are excluded. The troubleshooting guide explains scope
and download templates. Hosted execution of the new checkpoint remains pending.

All prior workflow steps and the original two-process, mesh-scale 4.0, four-AMR
benchmark remain unchanged. Numerical code, scientific thresholds and generated
status documents were not modified. This is an evidence-collection improvement,
not completion of Palace repair or independent scientific validation.

## Verification

- Initial full `uv run --no-sync pytest`: exit 1; 12 failed, 1,948 passed,
  11 skipped. Git-related tests hit the system launcher's unaccepted Xcode
  license; doctor therefore reported an unknown commit. The focused unchanged
  `test_actual_commit_discovery_accepts_head` probe reproduced that failure.
- With `PATH=/Library/Developer/CommandLineTools/usr/bin:/opt/homebrew/bin:$PATH`,
  full `uv run --no-sync pytest` passed twice unchanged: **1,960 passed,
  11 skipped**, exits 0, 62.87 and 61.41 seconds. No license was accepted,
  no tests disabled and no product change made for the environment issue.
- `uv run --no-sync ruff check .`, `uv run --no-sync mypy`, `uv build`: passed.
- README, namespace, generated-status, strict benchmark, public-claim, canonical
  evidence, Palace AMR consistency, showcase, cross-artifact, retained Palace
  evidence and plugin-bundle checks passed. Exact commands, exits and logs are
  in the evidence packet. Passing consistency gates does not remove Palace's
  existing scientific blockers.
- YAML parse, every workflow Bash block's syntax, unchanged prior steps and
  checkpoint ordering passed twice. Actual GitHub artifact upload remains
  untested until the dispatched Linux job reaches it.
- The audit generator emitted host-specific tracked snapshots; those changes
  were preserved in a temporary patch then restored to the committed versions,
  keeping this change focused. No unrelated edits were present or discarded.
- Test outputs were retained separately; canonical JUnit/status was not replaced
  for a workflow-only change.

## Git, CI and current job

Previous main `e121c01` CI `35457250961` and test `35457250984` both passed.
New main `034fc15` [CI 35914914674](https://github.com/JungluChen/Text-to-Layout/actions/runs/35914914674)
and [test 35914914651](https://github.com/JungluChen/Text-to-Layout/actions/runs/35914914651)
were initially in progress. A later inspection confirmed the test workflow passed;
the platform CI remained in progress.

Cherry-picked only this collection/guide change onto the already validated
native-discovery candidate and pushed `494a51a2d939d0acbe351aad9862d4c696eae35c` on
`codex/palace-native-discovery-20260915`. Candidate numerical code is unchanged
from `8705804` and remains outside main. Its new [CI 35915000369](https://github.com/JungluChen/Text-to-Layout/actions/runs/35915000369)
is pending; the 1,960-test results above apply to main, not this branch's
historical 1,969-test suite.

After confirming every existing Palace run completed, dispatched exactly one
[Palace run 35915053169](https://github.com/JungluChen/Text-to-Layout/actions/runs/35915053169)
at candidate `494a51a`, job **107364317886**. It was initially installing Palace via Spack; the completed outcome below supersedes that snapshot.
The workspace is back on main. No local solver or duplicate remote run started.

## Initial continuation snapshot (superseded by completed outcome below)

```sh
gh run view 35915053169 --repo JungluChen/Text-to-Layout
gh run view 35915000369 --repo JungluChen/Text-to-Layout
```

Work in `/Users/ludko/Desktop/text2layout/Text-to-Layout`. Monitor the existing
job, allowing its configured 300-minute limit; do not launch a duplicate. First
confirm that `palace-pre-benchmark-1` exists and inspect its hashes, actual smoke
outputs and initial resources. Download final compact evidence if available.
If the runner disappears again, compare attempt-specific logs and add measured
in-run resource/log diagnostics before guessing at a numerical fix. Initial
resource availability alone cannot diagnose peak-memory failure.

Retain real benchmark invocation, outputs, convergence and reference comparisons;
repeat original verification before promoting the discovery fix. No new numerical
frequency, accuracy error, convergence or commercial parity is claimed today.
Inspect subsequent documentation-commit CI independently. Items 3–9 remain in
their established order; UI screenshots and host certification remain pending.

The automation remains ACTIVE, daily 01:00 Asia/Taipei. The latest wake arrived
at 03:16:56; this record does not claim exact scheduler punctuality. Next regular
occurrence is 2026-09-25 01:00 Taipei. Network timeout handling now bounds remote
read subprocesses so a request does not wait indefinitely.

Evidence: [manifest](evidence/2026-09-24-031656/manifest.json).

Record prepared: 2026-09-24T04:21:32.397573+08:00.

## Completed outcome and current continuation (September 24, 07:20 Taipei)

The evidence-collection fix was verified on the actual Linux runner. Palace run
`35915053169` uploaded **16 files**, artifact **10778525629**, named
`palace-pre-benchmark-1`, before its reduced benchmark. The archive digest is
`5dfd9c1afbd12ef3fffe3a384cb4f7bb9c036ac8051406b4e93a5fb0d0eeed5d` as reported
by GitHub. Download succeeded; files are retained under this report's evidence
`checkpoint/` directory with individually verified SHA-256 entries.

The retained smoke result is Palace 0.17.0, commit
`12d8069afb5aa9e169a17e303d735e120968e9f2`, exit 0, runtime about 13.60 seconds.
Five frequencies were parsed (lowest 0.103934328377 GHz). Both solver-output
hashes and the retained cavity configuration input hash match `result.json`.
The mesh input is hashed but not included in this compact packet; retrieve it
from the pinned upstream smoke source before claiming full input replay.
These are official cavity smoke outputs, not reduced CPW results or reference
agreement. No new convergence or accuracy claim follows.

Starting runner resources were 15,989 MiB RAM, 14,892 MiB available, 3,071 MiB
swap and 82 GiB free disk as displayed by the runner utilities. These initial
values cannot establish peak usage or an OOM. The reduced step began at
21:36:33 UTC on September 23; the runner reported shutdown at 21:54:01 UTC,
exit 143. The final benchmark packet was not uploaded. The cause remains unknown.
The early smoke checkpoint survived this failure, satisfying the bounded
collection improvement but leaving Palace scientific validation blocked.

Implementation `034fc15` main CI/test both passed; isolated candidate `494a51a`
CI passed. Reporting commit `86425bc1dcde9181057de3e1f8515c830fcc1a39` was pushed
successfully and its test workflow `35915634168` passed. Its platform CI
`35915634148` failed only on Windows/Python 3.11:
`test_job_start_collect_records_return_code_and_outputs` still observed
`running` after the existing 15-second polling interval (1 failed, 1,956 passed,
14 skipped in that job). Other platform jobs passed. This alone does not prove
a code defect; no timeout or assertion was changed. The required unchanged
failed-job rerun was accepted as **attempt 2**, still at SHA `86425bc`, and is
in progress. Compare outcomes before editing; if inconsistent, investigate
Windows scheduling/resource differences and retain diagnostics.

One final read-only approval review timed out; its explicitly permitted single
retry succeeded. No current approval block remains.

**Current next actions:**

```sh
gh run view 35915634148 --repo JungluChen/Text-to-Layout --attempt 2
gh run view 35915053169 --repo JungluChen/Text-to-Layout
```

Do not launch another identical Palace run just to repeat the now-observed
runner loss. Design and validate a focused in-run resource/log checkpoint on
the isolated candidate, preserving the original numerical settings and solver
exit semantics. Capture process/cgroup memory, MPI/solver stdout and stage
progress before interruption; diagnose from those measurements. Main retains
only the collection change; native discovery still awaits valid reduced evidence.
All required solver/convergence/reference gates remain unchanged. No active
Palace job remains; the Windows unchanged CI rerun is the active continuation.
