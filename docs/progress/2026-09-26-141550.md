# Palace bounded AMR failure: repeated diagnosis and candidate experiment

**Document class: MANUAL_DOCUMENTATION.** Run began 2026-09-26 14:08 Asia/Taipei on macOS arm64. Main was clean at `4197b2ae995801f1157b50ef5cab86d9a8e068af` and matched `origin/main`. The isolated Palace branch started at `428b28a`. No local Palace process was launched; the real solver is available on the Linux GitHub runner.

## Reproduction and scientific finding

The user-reported [Palace run 36148860525, attempt 2](https://github.com/JungluChen/Text-to-Layout/actions/runs/36148860525) at `2b883601bd96439309fe6414c813d0eefa909ec5`, job `108305501525`, reproduced attempt 1 without a source/configuration edit. Installation, official smoke, base mesh, and Palace's bounded process succeeded in both attempts. The base mesh hash was `dd9cc2e04f05385aa4a6d30c9fc3d3f435fcc466f3a343a614d82a292d1d4069` in both, with 106,439 generated elements; Palace's input preprocessing counted 106,614. The executable SHA-256 was `ad43ec030f51435f32150a5c72cb324ca03083d2ffc2f91405bbf41c6bc2240f` in both. The AMR input hash was `f8b6c1ef285e647a811f247b2213630c293fdebd1a20a7417ea8dfbc40f66440` in both. Its `UpdateFraction` was 0.7; Palace reported marking 9,812/106,614 elements for 70% of the error, then conforming refinement added 327,613 elements to reach 434,227. Both attempts ended `SIMULATION_INVALID`: **434,227 > the 200,000-element diagnostic limit**. The first and second solved-state eigenvalue CSV hashes also matched across attempts. This is a repeatable configured resource-bound failure, not the Node.js 20 or forthcoming Ubuntu-label warnings in the annotations.

The attempt-2 Palace process reported peak memory 7,838,315,315 bytes against a configured 8,589,934,592-byte limit; local runner sampling recorded 7,540 MiB solver RSS. The full reduced benchmark, numerical convergence, and independent frequency comparison were **not run**. Two eigenvalue rows and a zero process exit do not establish scientific validity. The source files and compact records retained under [attempt-2 evidence](evidence/2026-09-26-141550/palace-attempt2/) preserve the invalid verdict, resolved configuration, mesh metrics, resource budget, solver record and eigenvalues. GitHub artifact `10895507351` is the original bounded packet; the base mesh, pre-benchmark and integration packets are `10895138217`, `10894444897`, and `10895941366`.

## Candidate change and verification

On `codex/palace-native-discovery-20260915`, implementation commit `f407c6330e2e36de1fc70024211669ede450ac3d` changes only the bounded diagnostic invocation to `--amr-update-fraction 0.1`. Palace's documented AMR marks elements by error indicator; the retained log confirms the 0.7 setting led to the excessive conforming-refinement jump. This is a **resource-budget experiment** to make one diagnostic refinement smaller. It does not change the 200,000-element guard, full reduced benchmark's 0.7 setting, physical model, or acceptance criteria. It is not a Palace fix until real reruns pass, and smaller marking alone cannot establish convergence.

Local candidate checks: direct full pytest with the actual project source and Command Line Tools Git, permitted loopback sockets: **1,978 passed, 12 skipped**; Ruff passed; mypy passed on 182 source files; offline non-isolated uv sdist/wheel build passed using the installed Hatchling. README claims, namespace, generated project status check, strict benchmark, canonical evidence, Palace AMR gate, showcase drift and cross-artifact consistency checks all returned zero. The Palace gate still reported seven scientific blockers. Initial sandboxed `uv run` attempts could not resolve Hatchling due restricted DNS; an initial direct pytest without developer-tool selection/loopback permissions had 14 environment-related failures. The rerun with `DEVELOPER_DIR=/Library/Developer/CommandLineTools`, current source `PYTHONPATH`, and loopback access passed. These environment failures were not treated as product defects.

The candidate was pushed to its isolated branch after fetching origin. [Real-solver run 36223218053](https://github.com/JungluChen/Text-to-Layout/actions/runs/36223218053), job `108352252265`, was dispatched at the exact candidate SHA and was in progress when this report was first written. No Palace candidate was merged to `main`. Its bounded diagnostic must pass twice, followed by the unchanged full reduced benchmark, convergence, source-backed tolerance and retained solver evidence. If the first new run fails, compare its actual log and outputs before another edit. A green generic platform matrix does not substitute for Palace validation.

## Precise continuation

1. Inspect `gh run view 36223218053 --json attempt,status,conclusion,headSha,jobs,url` and download its numbered bounded diagnostic artifact once complete. Compare solver binary SHA, base mesh hash, marked and final element counts, peak memory, eigenvalues, stage verdict, and the full benchmark result with the two retained attempts. Do not start an overlapping Palace job.
2. If the candidate reaches the bounded stage, rerun the same workflow unchanged at `f407c63` for the repository's second post-edit verification; investigate disagreement before editing. Do not promote a bounded-only pass as a scientific result.
3. Run and inspect full reduced benchmark, AMR and domain convergence, mode identity, public-reference comparison with justified tolerance, and exact-SHA platform CI before merging a verified Palace fix to main. Keep item 2 in progress otherwise. Update this report or a subsequent dated report with run/job IDs, all failures or skips, commits and exact next action.
