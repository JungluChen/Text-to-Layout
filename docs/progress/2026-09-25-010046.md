# Stream Palace runtime observations before runner loss

**Document class: MANUAL_DOCUMENTATION.** Wake: 2026-09-25 01:00:46 Asia/Taipei;
first local check 01:01:04. Backlog item 2, local Apple Silicon Mac, Python 3.12.14.
Starting main `0378736d69a31e71aca134e38f2dd15ecc6342a6` was clean and matched
fetched origin. Repository instructions, diagnostic policy, backlog, prior report,
requirements, design brief, catalog and guidebook were reviewed. No active local
solver/Spack/pytest process or remote Palace job preceded the new dispatch.

## Existing evidence and diagnosis

The previous run retained a successful official Palace 0.17.0 smoke checkpoint,
then lost the runner during the unchanged reduced CPW benchmark. Two earlier
candidate attempts had the same interruption without artifacts. The early
upload fix is verified; the cause of runner loss, reduced numerical result,
convergence and independent comparison remain unknown. See the September 24
report and hashed solver-owned smoke CSVs. No repeated blind Palace retry or
speculative numerical change was made today.

Windows CI is inconsistent: run `35915634148` at `86425bc` passed on its unchanged
second attempt. Run `35933303913` at `0378736` failed on the same Windows/Python
3.11 job-status assertion (still running after the existing 15-second polling
window), while its other platform jobs passed. Its required unchanged failed-job
rerun was requested today and also **passed as attempt 2**. Those successes do
not explain the first-attempt failures. Tests/timeouts remain unchanged; retain
and compare Windows worker startup/scheduling diagnostics if this recurs rather
than treating a retry as a code fix. Main test `35933303915` and nightly
`35974063973` passed. Failure and rerun records are retained in this report.

## Implemented diagnostic scope

Main implementation **`4dfc1f2e4693f896985f9f02e84e96d14619bc8a`** was pushed to
`origin/main`. `scripts/observe_palace_ci.py` samples every 30 seconds alongside
the original benchmark. It streams JSON lines to Actions and writes
`out/toolchain/palace_runtime.txt`. Samples include `/proc/meminfo`, the observer's
cgroup-v2 membership/current/peak/limit/events when available, twelve highest-RSS
processes (names, not arguments), and bounded 4 KiB tails of selected toolchain,
resource, base-mesh/configuration and solver stdout/stderr records.

The observer reads existing files/process data; it does not own, start, stop or
validate a solver. It reports missing counters/files explicitly, marks truncated
tails, and never changes solver settings or thresholds. Shell cleanup stops only
its observer while preserving both successful and failing benchmark exit codes.
The troubleshooting guide documents use and interpretation. These are diagnostics,
not a replacement for full solver outputs or scientific acceptance gates.

The same verified collection change was cherry-picked and pushed as
**`39daad425e3437a774d4a72c898b44773653e58e`** on the existing isolated
`codex/palace-native-discovery-20260915` branch. Native discovery remains outside
main, and its numerical code is unchanged. The shared checkout returned to main.

## Verification and corrections

| Check | Observed result |
| --- | --- |
| Observer focused suite, repeated unchanged | 6 passed twice, 0.30 and 0.31 seconds |
| Full `uv run --no-sync pytest --junitxml=/private/tmp/sep25-test-report.xml` | **1,966 passed, 11 skipped**, 52.05 seconds, exit 0 |
| Ruff (whole tree and explicit observer script) | Passed |
| Strict product mypy and `uv build` | Passed |
| README, namespace, generated-status and strict benchmark gates | Passed after sequential README repeats described below |
| Canonical, Palace AMR, showcase, cross-artifact and retained Palace evidence | Consistency checks passed; existing scientific blockers remain |
| Public-claim audit, output under `/private/tmp/sep25-audit` | Passed; committed audit snapshots untouched |
| Plugin bundle check and all workflow Bash syntax | Passed |
| Regenerated status and status/claim regression tests | Generator drift check passed; 42 tests passed |

Six added tests exercise bounded/partial UTF-8 reads, missing files, cgroup scope,
one-shot capture, invalid intervals and actual shell cleanup with harmless exit
0/7 probes. These probes never impersonate solver output. Two Bash lifecycle
cases are explicitly scoped to POSIX; Windows does not run the Linux-only CI
observer shell. No existing product test was disabled.

One README gate was initially run concurrently with pytest and failed on missing
tile summaries. Investigation found the existing `test_tile_map_without_report_summary_fails_validation`
temporarily removes those summaries and restores them in `finally`. The gate
passed twice unchanged after pytest finished; tracked showcase files were clean.
No documentation patch was used to hide the failed gate. Avoid parallel tests
and claim checks that share these temporary fixtures. The original gate failure
is retained alongside subsequent success. An early lint invocation also hit the
sandboxed default uv-cache path; authorized checks used the explicit temporary
cache and passed. No license acceptance or dependency change was needed.

The actual full-suite JUnit was copied to `out/evidence/test_report.xml`, then
`scripts/generate_project_status.py` regenerated the JSON and Markdown status.
It now records 1,966 passes/11 skips; no generated document was hand-edited.
Full commands, exit codes and logs: [evidence manifest](evidence/2026-09-25-010046/manifest.json).
The initial gate result list deliberately retains the concurrent README failure;
use its later sequential log and this explanation for the final outcome.

## Active validation and exact continuation

After a fresh check that prior Palace runs completed, dispatched exactly one
[Palace run 36032456522](https://github.com/JungluChen/Text-to-Layout/actions/runs/36032456522)
at candidate `39daad4`, job **107744315010**. It is in progress. Actual Linux
observer output is pending; do not claim the new stream is verified on that
runner yet. There is no new reduced benchmark result or accuracy/convergence claim.

Main implementation [CI 36032356510](https://github.com/JungluChen/Text-to-Layout/actions/runs/36032356510)
and [test 36032356513](https://github.com/JungluChen/Text-to-Layout/actions/runs/36032356513),
plus [candidate CI 36032400045](https://github.com/JungluChen/Text-to-Layout/actions/runs/36032400045),
were in progress at inspection. Later documentation commits need their own
exact-SHA checks. Successful pushes do not establish passing CI.

```sh
gh run view 36032456522 --repo JungluChen/Text-to-Layout
gh run view 36032400045 --repo JungluChen/Text-to-Layout
```

Continue in `/Users/ludko/Desktop/text2layout/Text-to-Layout`. Monitor the existing
job; do not dispatch another while it is active, and allow its existing 300-minute
limit. On completion retain full Actions logs, checkpoint artifacts, diagnostic
JSON lines and any actual benchmark outputs. Confirm observations contain real
solver stage/log progress, then compare cgroup counters and owned processes
across timestamps. Initial free memory or total process RSS alone does not prove
OOM; shared pages and cgroup scope matter. Diagnose the failure before changing
numerical settings or limits. Preserve the discovery candidate until real reduced
execution, convergence and reference evidence support the corresponding promotion.

The automation remains daily at 01:00 Asia/Taipei; next regular occurrence is
2026-09-26 01:00. No local solver remains active. Lower-priority product surfaces,
commercial comparisons and screenshots retain their existing pending status.

Record prepared: 2026-09-25T01:13:12.537618+08:00.
