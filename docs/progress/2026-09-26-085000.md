# Integration inventory and Palace diagnostic checkpoint

**Document class: MANUAL_DOCUMENTATION.** Run began approximately 2026-09-26
08:50 Asia/Taipei; checkpoint 09:04. Host: macOS arm64, Python 3.12.14,
uv 0.12.13. Starting `main` and `origin/main` were
`d8ea3ce614a4e6a62a63ff6d61a61bd9f8857c2e`. Priority 0 was selected
first; item 2's existing Palace run was inspected before launching work. The
five most recent user-pasted continuation files have identical SHA-256
`b273a59bfe88846e650516df8c061d786215d398a94ffa2584f25bb0f63ab9ff`.
The later 100-target mission is reflected in the current backlog and catalog.

## Delivered behavior and boundary

Implementation `10fc549f44e201165e278c6d50a2c0395955e5a9` preserves the
user's 100 numbered integration candidates and ten pillars in package data.
The 60 supplied source hints remain unverified; 40 slots have no source hint.
No candidate is certified open-source, installed, supported or scientifically
valid by this catalog. Existing `SolverAdapter` and canonical evidence models
remain authoritative. The separate `simulation/backends` namespace registers
no tool. Optional groups cover declared Python dependencies; empty native and
commercial groups install no solver and grant no license. The base dependency
set is unchanged, so the lean/headless install requirement remains open.

The exact guidebook command `uv run textlayout doctor --json` succeeded. On
this Mac, `integration_targets` contained 100 entries: 92 `NOT_PROBED`, five
`MISSING` and three `FOUND` under eight exact existing probe mappings. All 100
had `source_verified=false`, `license_verified=false` and
`execution_verified=false`. These are discovery results, not solver execution
or platform certification. The built wheel includes `targets.json`.
Requirements, architecture, function catalog, guidebook and top backlog were
updated with those limits. The daily conversation automation was updated from
50/seven to 100/ten candidates while retaining its active 01:00 Asia/Taipei
schedule. Its next scheduled occurrence is 2026-09-27 01:00 Asia/Taipei, if
the Mac remains on and Codex is running.

## Palace scientific evidence and unresolved failure

[Run 36148860525, attempt 1](https://github.com/JungluChen/Text-to-Layout/actions/runs/36148860525)
at candidate `2b883601bd96439309fe6414c813d0eefa909ec5` completed failed at
job `108116750266`. Pinned Palace 0.17.0 installation, official smoke,
base mesh, and bounded Palace process all completed. The base mesh contained
106,439 elements. Palace's AMR result reached 434,227 elements, exceeding
the declared 200,000-element guard. The process returned 0, but the benchmark
correctly marked `SIMULATION_INVALID`; the full reduced benchmark,
convergence and reference comparison were skipped. Solver SHA-256 was
`ad43ec030f51435f32150a5c72cb324ca03083d2ffc2f91405bbf41c6bc2240f`.
Reported Palace peak memory was 7,945,689,497 bytes; the configured process
RSS limit was 8,589,934,592 bytes. Neither process exit nor these two AMR
frequency rows establish a valid result. Do not relax the guard on this basis.

The original GitHub diagnostic artifact is `10873339434`; supporting smoke,
mesh and pre-benchmark artifacts are `10873587680`, `10873636070` and
`10874010399`. Ten compact solver-owned records are retained under
[`evidence/2026-09-26-085000/palace`](evidence/2026-09-26-085000/palace).
The retained raw stage failure reports the precise 434,227/200,000 condition;
`canonical_evidence.json` retains status and solver identity. The unchanged
workflow was rerun as **attempt 2**, job `108305501525`, on the same candidate
SHA after confirming no Palace job was active. It was in progress at this
checkpoint. No candidate Palace code was promoted to `main`.

## Verification and publication

| Command or gate | Result |
| --- | --- |
| `uv run --frozen pytest --junitxml=out/evidence/test_report.xml -q` | Exit 0; **1,973 passed, 11 skipped** in 48.72 s. The skips remain skips. |
| Focused doctor/catalog pytest after build | Exit 0; 11 passed. |
| Documentation, README and claim tests after report edits | Exit 0; 52 passed. |
| `uv run --frozen ruff check .` | Exit 0. |
| `uv run --frozen mypy` | Exit 0; 185 source files. |
| `uv build --offline` and wheel package-data check | Exit 0; sdist/wheel built, catalog JSON present. |
| `uv lock --check --offline` | Exit 0; 230 packages resolved against lock. |
| Exact guidebook doctor command | Exit 0; candidate counts above. |
| Generated status check, namespace, README claims, strict benchmarks, canonical evidence, Palace evidence, showcase, consistency, plugin bundle | Exit 0. The Palace evidence gate still reports seven blocking scientific gates. |
| Claim downgrade audit | Exit 0; generated local audit snapshots showed unavailable Docker/fresh-install evidence and were not published as changed product evidence. |
| Actual 100-target solver, desktop and commercial comparisons | Untested; this catalog does not supply them. |

The project status and canonical JUnit were regenerated by the prescribed
generator and included in the implementation commit. The bounded catalogue
tests and package check are structural checks; they do not establish physics
accuracy. Prior exact-SHA main [CI 36151512846](https://github.com/JungluChen/Text-to-Layout/actions/runs/36151512846)
and [test 36151512922](https://github.com/JungluChen/Text-to-Layout/actions/runs/36151512922)
passed at `d8ea3ce`.

Implementation commit `10fc549f44e201165e278c6d50a2c0395955e5a9`
was pushed directly to `origin/main` after fetching and verifying ancestry.
Its exact-SHA [CI 36206960023](https://github.com/JungluChen/Text-to-Layout/actions/runs/36206960023)
and [test 36206959952](https://github.com/JungluChen/Text-to-Layout/actions/runs/36206959952)
were in progress at this checkpoint. This subsequent report and compact
Palace records are a separate documentation commit; inspect its own CI too.

## Precise continuation

1. From `/Users/ludko/Desktop/text2layout/Text-to-Layout`, inspect
   `gh run view 36148860525 --json attempt,status,conclusion,jobs,headSha,url`
   with `/Library/Developer/CommandLineTools/usr/bin` first on `PATH`.
   If attempt 2 fails the same 200,000-element guard, compare its base mesh,
   AMR outputs, memory and hash records with attempt 1. Only then investigate
   mesh sizing and refinement on the isolated Palace candidate branch. Keep
   the published scientific acceptance limit and rerun original verification
   twice after a focused change. If the outcomes differ, diagnose the source
   of nondeterminism first.
2. Inspect exact-SHA CI and test for `10fc549`, then for this report commit.
   Treat pending checks as pending and repair an actual failure with the
   repository's reproduce-before-edit sequence.
3. Continue Priority 0: verify canonical upstream/license/aliases for the 100
   slots; map one already implemented backend through the existing adapter
   and evidence contract, with a public reference and justified tolerance.
   Then address the lean core dependency migration separately. Keep
   guidebook and function catalog synced with each real capability.
