# Palace native-discovery candidate

**Document class: MANUAL_DOCUMENTATION.** Started 2026-09-15 13:21:23
Asia/Taipei on the existing macOS arm64/Python 3.12.14 host. Backlog item 2.
Initial local/remote main: `604f9bc9af6068e7afd1e8f7870d64a736b77502`.
Its [CI](https://github.com/JungluChen/Text-to-Layout/actions/runs/34930974052)
and [Windows test workflow](https://github.com/JungluChen/Text-to-Layout/actions/runs/34930974066)
both passed. Initial process/workflow inspection found no active Palace,
Spack or project pytest jobs. The prior failed solver run and its retained
artifact were reused as diagnostic evidence.

## Reproduction, diagnosis and candidate

The failed Linux run `34824773767` installed Palace 0.17.0 and passed the
official smoke test using a native Spack path stored in `install.json`.
The subsequent reduced benchmark returned `SKIPPED_SOLVER_ABSENT`.
`detect_palace` read that record only for paths beginning `wsl:`. A native
Spack executable outside PATH was therefore missed despite successful smoke.

Before changing tracked files, ran the same isolated native-record discovery
probe twice. Both runs failed identically: explicit-path discovery returned
available, record-based discovery returned unavailable. The install-record
SHA-256 remained `f0392b81f1dbe78dddeebf4af01bbf9cd76d51074614efc1994fa18c53383a76`.
The probe uses an inert file solely to test path discovery with version probing
disabled. **No fixture was executed and it is not scientific solver evidence.**

Candidate commit `8705804045700d4b2bd87bd1d8e4c79839fd955d` on
`codex/palace-native-discovery-20260915` accepts an absolute native record only
when it identifies an existing executable file. Explicit arguments and
`TEXTLAYOUT_PALACE` retain priority. Malformed or stale native records leave
normal discovery available. Existing WSL handling and scientific thresholds
are unchanged. The original probe passed twice after the fix with the same
fixture/input hash. Nine new regression cases cover native identity, caller
overrides and malformed/stale records.

## Verification

Commands ran from `/Users/ludko/Desktop/text2layout/Text-to-Layout`, using
`PATH=/Library/Developer/CommandLineTools/usr/bin:$PATH` for the working Git
executable and `UV_CACHE_DIR=/private/tmp/textlayout-uv-cache`. Exact commands,
exit codes, source/input hashes, OS, Python/dependency versions, timestamps,
runtime and complete outputs are in the [evidence manifest](evidence/2026-09-15-132123/manifest.json).

| Check | Result |
| --- | --- |
| Native-record probe before change | Failed twice, same cause/input hash |
| Identical probe after change | Passed twice |
| Palace installation tests | 13 passed, 1 skipped |
| Full `uv run pytest --junitxml=out/evidence/test_report.xml` | 1,969 passed, 11 skipped; 518,005 existing warnings |
| Ruff / mypy / `uv build` | Passed; mypy covers 182 source files |
| Generated status | Regenerated by prescribed script; drift check and 22 status tests pass |
| Namespace / README / strict benchmark / public claim gates | Passed |
| Canonical / Palace retained evidence / cross-artifact / showcase gates | Passed consistency checks |
| Plugin bundle / acceptance / benchmark determinism | Passed; artifact regeneration leaves tracked acceptance/benchmark directories unchanged |

One initial focused command named a nonexistent `test_palace_capability.py`
and exited 4 without running tests. Corrected the command to the existing
installation test file; no test was disabled or altered to hide that invocation
mistake. The full suite also includes the existing Palace backend tests.

Palace's existing scientific evidence still has seven blocking gates (AMR,
element indicators, frequency/global error convergence, domain sweeps/sensitivity
and independent reference). Passing consistency checks means these limitations
are reported honestly; it does not mean the benchmark is scientifically validated.
No new solver output, frequency result, comparison error or convergence result
is claimed from this local discovery fix.

## Remote validation and Git state

The locally verified candidate was pushed to its isolated validation branch.
The shared workspace was returned cleanly to main. Keeping the candidate off
main allows the actual Linux solver workflow to validate it before promotion;
this main-branch change is a substantive documentation/evidence checkpoint.
Only one Palace workflow was dispatched, with the original benchmark enabled:

```sh
gh workflow run palace-integration.yml --repo JungluChen/Text-to-Layout --ref codex/palace-native-discovery-20260915 -f run_benchmark=true
```

- [Palace run 34932740535](https://github.com/JungluChen/Text-to-Layout/actions/runs/34932740535),
  job `104264198805`, candidate SHA `8705804`: **in progress**.
- [Candidate CI 34932724963](https://github.com/JungluChen/Text-to-Layout/actions/runs/34932724963),
  same SHA: **in progress** at checkpoint preparation; quality-gates and bundle-sync passed.

The Palace workflow includes pinned installation verification, official smoke,
the original reduced CPW benchmark, real-invocation/hash checks and compact
artifact publication. Allow its existing 300-minute job limit to govern it;
do not terminate it at one hour or launch a replacement while it is active.
Its result must be inspected, not assumed from successful dispatch.

## Exact continuation

```sh
gh run view 34932740535 --repo JungluChen/Text-to-Layout
gh run view 34932724963 --repo JungluChen/Text-to-Layout
```

If still running, monitor these IDs. If failed, retrieve `--log-failed` and
download `palace-integration-evidence` to a new run-specific directory. Compare
installation identity, invocation, outputs, hashes and convergence. Reproduce
the smallest faithful failure twice before another code edit. Do not relax
scientific gates or turn an absent solver into a passing simulation.

If execution succeeds, retain and assess the actual numerical evidence and
repeat the original verification before accepting the fix. Fetch current main,
review candidate `8705804`, reconcile concurrent changes, and promote only the
verified scope to origin/main without force-pushing. Any later scientific
blocker keeps item 2 open even if discovery is repaired. Preserve the candidate
branch and these records until work is integrated.

No active local solver processes belong to this run. The daily schedule remains
01:00 Asia/Taipei; next regular occurrence is 2026-09-16 01:00. UI, commercial
comparisons and platform/host milestones remain in their existing priority order.

Checkpoint timestamp: 2026-09-15T13:30:44+08:00 (Asia/Taipei). Job snapshots are retained beside the local evidence.
