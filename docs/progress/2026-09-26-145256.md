# Release gates and 100-target checklist checkpoint

**Document class: MANUAL_DOCUMENTATION.** Run began 2026-09-26 after the user
requested the recent Palace branch merge, 100 open-source integrations,
Windows/macOS/Linux apps with upgrades, PyPI/npm delivery and a paper-backed
tutorial. Host: macOS arm64, Python 3.12.14. Main started clean at `4171905`.

## Merge and numerical status

Fetched branch `codex/palace-native-discovery-20260915`; its latest candidate
was `f407c6330e2e36de1fc70024211669ede450ac3d`. Exact-SHA generic CI
[36223212504](https://github.com/JungluChen/Text-to-Layout/actions/runs/36223212504)
passed, but real Palace [run 36223218053](https://github.com/JungluChen/Text-to-Layout/actions/runs/36223218053),
job `108352252265`, was still installing the pinned solver at this checkpoint.
The new bounded AMR setting has not executed there. Two earlier unchanged
attempts failed the 200,000-element guard. Therefore the Palace branch was
**not merged**: a green generic matrix is not numerical validation. The next
action is to inspect the real solver result and artifacts, rerun unchanged on
the exact candidate SHA if it passes, then require the full benchmark,
convergence and independent public-reference comparison before merge.

## Delivered release and inventory preparation

- `docs/integrations/checklist.md` renders all 100 requested slots from the
  inventory. It reports **0/100 fully accepted**; existing exploratory
  adapters remain separate. Completion requires a full implementation commit
  plus retained source/license, execution, reference and platform evidence.
  The generator rejects unknown IDs, malformed hashes and missing evidence
  paths, and CI checks for drift. This does not install 100 solvers.
- `.github/workflows/release.yml` now gates Python publication on Python 3.12
  pytest/Ruff/mypy/build/CLI/doctor checks on Ubuntu, Windows and macOS,
  separate evidence checks, a version-matched tag on `main`, and upload of the
  verified sdist/wheel. It retains PyPA's official OIDC trusted-publishing
  action rather than a long-lived `PYPI_API_TOKEN`. GitHub release creation
  depends on PyPI success. [PyPI trusted publishing documentation](https://docs.pypi.org/trusted-publishers/using-a-publisher/)
  explains the required project/workflow binding. A read-only request to
  `https://pypi.org/pypi/text-to-gds/json` returned HTTP 404 today; the package
  is not yet publicly available under that name. No version tag was created,
  and the publisher binding remains unverified.
- `docs/releases/README.md` and requirements distinguish the buildable Python
  CLI from absent native desktop installers, update channels and npm client.
  Automatic upgrades need signed update metadata, rollback and installed-app
  tests; npm publication needs an actual tested JS client and package identity.
  No desktop or npm release was claimed or published.
- `docs/guidebook/scientific-result-check.md` pairs an existing CPW geometry
  illustration, exact retained failed Palace evidence, step-by-step commands,
  an AI prompt and primary literature. [Khalil et al.](https://doi.org/10.1063/1.3692073)
  concerns measured resonator quality-factor extraction; it is explicitly not
  misused as a matching Palace eigenfrequency reference. A valid public
  frequency target and real versioned desktop screenshots remain pending.

## Verification and limits

Local full pytest: **1,973 passed, 11 skipped** in 43.17 s. Ruff and strict
mypy passed (185 source files). Offline non-isolated `uv build` produced wheel
and sdist. The generator `--check`, YAML job parse, release tag match
`v0.3.0`, and deliberate `v0.2.0` rejection behaved as expected. README,
namespace, generated project status, strict benchmark, canonical evidence,
Palace, showcase and consistency gates passed as commands. The Palace gate
still reported **seven scientific blockers**, so its zero exit is not an
accuracy pass. Actual release workflow publication, installed-app matrix,
auto-update, npm, commercial comparisons and 100 candidate executions were
not run.

## Continuation

1. Inspect [Palace 36223218053](https://github.com/JungluChen/Text-to-Layout/actions/runs/36223218053)
   without dispatching a duplicate. Compare the new bounded artifact with
   the two prior attempts. Complete the repeat and full scientific gates before
   merging its branch. Keep unvalidated changes isolated.
2. Verify exact-SHA CI for the commit containing this checkpoint. Configure
   the `text-to-gds` PyPI trusted publisher for `JungluChen/Text-to-Layout` /
   `release.yml` when a release is scientifically and operationally ready,
   then test installation from the published wheel. Do not create a tag yet.
3. Design and test the first real desktop shell and npm client against the
   shared core, with platform screenshots and auto-update/rollback tests.
   Audit each checklist target and accept it only with the required evidence.
