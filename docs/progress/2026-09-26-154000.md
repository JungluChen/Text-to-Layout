# Palace bounded mode-classification checkpoint

**Document class: MANUAL_DOCUMENTATION.** Run started 2026-09-26 15:34 Asia/Taipei;
checkpoint 15:40. Selected backlog items: Priority 0's accuracy-first Palace
slice and item 2. Main began clean at `9f7379a5d954bbc424b7032084787351901b1267`
on macOS arm64. The isolated Palace branch remained clean at
`f407c6330e2e36de1fc70024211669ede450ac3d`. No solver ran locally;
the real solver ran on GitHub's Linux x86_64 runner, Python 3.12.3, Palace
0.17.0 (`ad43ec030f51435f32150a5c72cb324ca03083d2ffc2f91405bbf41c6bc2240f`),
Gmsh 4.15.2 and Open MPI 4.1.6.

## Scientific finding and evidence

[Palace run 36223218053, attempt 1](https://github.com/JungluChen/Text-to-Layout/actions/runs/36223218053)
completed **failed** at job `108352252265`, step 15. Installation, pinned
executable verification, official smoke, and base-mesh checkpoint passed.
The bounded diagnostic launched the actual Palace process with one AMR
refinement and `UpdateFraction=0.1`. The generated base mesh had 106,439
elements. Palace solved states at 106,614 and 114,276 elements, below the
predeclared 200,000-element resource guard; peak solver RSS was 1,973 MiB
in the retained observer report. The two reported eigenfrequencies were
5.096192768261 and 5.691721374551 GHz. These are solver outputs, **not**
accepted resonator frequencies: the classifier labeled the first `UNKNOWN`
and second `SUBSTRATE_MODE`. The first failed the quarter-wave profile gate;
the second also failed endpoint, localization and package-energy gates.
`canonical_evidence.json` reports `SIMULATION_INVALID` with
`ambiguous_mode_identity: physical target classification failed:
TARGET_MODE_NOT_FOUND`. Neither numerical convergence nor comparison with a
public frequency reference was reached. No HFSS/Keysight comparison exists.

Compact attempt-1 evidence is retained under
[`evidence/2026-09-26-154000/palace-attempt1/`](evidence/2026-09-26-154000/palace-attempt1/).
In particular, `bounded_solved_states.json` SHA-256 is
`2601a538e4a3bcf7234bb3f65746906ffa949eab3b63617585a1d0c84e18b342`,
`mode_tracking.json` is
`8d265d6bec31526116aee1d3874ab50bd80a4b2d638d2c9eb66a5d219e8e022f`,
and solver `eig.csv` is
`bf1532d3e70acaedf69281af898fab95c4cbe5aa6e3a583e16d2967f703ec888`.
The complete downloaded GitHub artifact is also at
`/private/tmp/textlayout-palace-36223218053` on this Mac. It includes
solver configuration, stage records, material map, energy table and the
process/resource logs. Source/geometry/material assumptions and the
predeclared classifier tolerances are in that artifact and the unchanged
branch source. The compact artifact does **not** retain the large raw VTK
field files or per-mode sanity/profile values, so the root cause of the
profile failure is presently unknown. Thresholds were not changed.

## Reproduction, checks and Git state

- `gh run view 36223218053 --job 108352252265 --log-failed` confirmed exit
  code 1 at `TARGET_MODE_NOT_FOUND`; `gh run download 36223218053 --name
  palace-bounded-diagnostic-1` succeeded before the rerun.
- `gh run rerun 36223218053` dispatched attempt 2 **unchanged** at exact SHA
  `f407c63`. At this checkpoint attempt 2 is installing Palace. It is the
  required second run before a model or classifier edit. Its result is pending.
- Previous unchanged SHA `2b88360` attempts both failed the AMR element guard
  at 434,227 / 200,000; see the prior report. The new 0.1 fraction reduced
  the count in this one attempt, but that does not validate the physics.
- No tracked product code changed here. Full pytest, Ruff, mypy, build and
  claim/evidence gates were **not rerun** for this documentation-only
  checkpoint; their most recent passed run is recorded in
  `docs/progress/2026-09-26-145256.md`. The full Palace benchmark and its
  convergence/reference gates remain **skipped by failed prerequisite**.
- The Palace branch was **not merged**. No desktop, npm, PyPI or 100-target
  completion claim follows from this solver attempt.

## Exact continuation

From `/Users/ludko/Desktop/text2layout/Text-to-Layout`, inspect the existing
attempt 2 without dispatching another job:

```sh
gh run view 36223218053 --repo JungluChen/Text-to-Layout --json attempt,headSha,status,conclusion,jobs
```

When its bounded artifact exists, download `palace-bounded-diagnostic-2`
and compare `bounded_solved_states.json`, `mode_tracking.json`, `eig.csv`,
`domain-E.csv`, Palace executable hash, material map and resource report
with attempt 1. If the failure repeats, retain both packets, diagnose the
physical field profile and field sampling, then make a focused instrumentation
or model change with a regression test. If results diverge, investigate
nondeterminism before editing. Preserve the 200,000-element guard and
quarter-wave acceptance criteria. The complete full benchmark must pass twice
with convergence and independently justified reference error before the
branch can be merged into `main`. Resume queued product work only with
accuracy status explicit; report pending platform/release evidence honestly.

This checkpoint's documentation commit, push and exact-SHA CI are to be
recorded in a subsequent report rather than self-referenced here.
