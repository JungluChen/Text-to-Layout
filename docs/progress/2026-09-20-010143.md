# Palace validation recovery and unchanged rerun

**Document class: MANUAL_DOCUMENTATION.** Scheduled wake: 2026-09-20 01:01:43
Asia/Taipei. Backlog item 2. Initial local and fetched remote main:
`a5d196ca653e97025dc9c7d096266019c74fc41f`; working tree clean.
Host remains the local Mac. Repository policy, backlog, prior reports,
requirements, design brief, function catalog and guidebook were reviewed.

## Confirmed state and scientific limits

Read-only account status confirmed ordinary execution is allowed again. No reset
credit was consumed or purchased. Remote and local job inspection found no active
Palace or Spack work before retrying. Candidate CI `34932724963` and main CI/test
`34933070760` / `34933070762` previously passed; no new test result is inferred.

Candidate `8705804045700d4b2bd87bd1d8e4c79839fd955d` remains on
`codex/palace-native-discovery-20260915`, separate from main. Its existing local
verification (1,969 passed, 11 skipped; Ruff, mypy, build and evidence checks)
is documented in `2026-09-15-132123.md`. Those tests were not rerun during this
documentation-only recovery. No code, numerical settings or scientific tolerance
was changed, and no generated status document was edited.

[Palace attempt 1](https://github.com/JungluChen/Text-to-Layout/actions/runs/34932740535/attempts/1)
passed installation and official smoke, then the runner shut down during the
reduced benchmark with exit 143. GitHub reports zero uploaded artifacts.
The surviving log does not establish a reduced-benchmark solver invocation,
parsed output, convergence or an independent reference comparison. It also
does not establish why the runner shut down. Preserve that uncertainty.

The installation record identifies Palace 0.17.0, upstream commit
`12d8069afb5aa9e169a17e303d735e120968e9f2`, Gmsh 4.15.2 and executable-path
SHA-256 `ad43ec030f51435f32150a5c72cb324ca03083d2ffc2f91405bbf41c6bc2240f`.
Full log, first-attempt run/job metadata and the empty artifact listing are
retained with hashes in [the manifest](evidence/2026-09-20-010143/manifest.json).

## Work performed

Recovered the missing scheduled-run records for September 16–19. They explicitly
separate observed execution/rejection results from scheduled notifications with
no recorded work; they do not retroactively claim commits or successful runs.

After confirming access recovery and no active Palace job, performed the
required unchanged rerun:

```sh
gh run rerun 34932740535 --repo JungluChen/Text-to-Layout --failed
```

GitHub accepted the request and confirmed **attempt 2**, still at candidate SHA
`8705804`, job **105933606196**, started at `2026-09-19T17:03:15Z`.
Run link: [34932740535, attempt 2](https://github.com/JungluChen/Text-to-Layout/actions/runs/34932740535/attempts/2).
This is a retry of the same failed job, not a duplicate while another is running.
The existing 300-minute job limit remains unchanged. A successful dispatch is
not a successful simulation; candidate promotion and backlog item 2 remain pending.

## Continuation and publishing

Evidence manifest hashes and retained byte counts were checked; report relative
links passed inspection. Attempt 2 metadata is retained as a snapshot, not a
completed result. The system Python launcher hit an Xcode-license error; these
checks used the existing project `.venv/bin/python` without accepting a license.

Main receives only these reports, retained diagnostic evidence and the backlog
update. No unvalidated candidate code is promoted. Checkpoint commit `1982ccd9eb5ec601be1708eefd7482c81ba51974` was pushed
successfully to `origin/main`; the working tree was clean afterward. Exact-SHA
[CI 35457221056](https://github.com/JungluChen/Text-to-Layout/actions/runs/35457221056)
was queued and [test 35457221033](https://github.com/JungluChen/Text-to-Layout/actions/runs/35457221033)
was running when inspected. These checks are pending, not passes. This publishing
record is added in a subsequent documentation commit to avoid self-reference.
Inspect any later documentation commit checks independently.

The latest retained attempt-2 snapshot shows installation via Spack still in
progress; smoke and the reduced benchmark have not yet reported results.

Start continuation from this working directory:
`/Users/ludko/Desktop/text2layout/Text-to-Layout`.
Use `/Library/Developer/CommandLineTools/usr/bin/git` if the system Git launcher
still requests an Xcode license.

```sh
gh api repos/JungluChen/Text-to-Layout/actions/runs/34932740535 --jq '{run_attempt,status,conclusion,head_sha}'
gh run view 34932740535 --repo JungluChen/Text-to-Layout
```

Monitor attempt 2/job 105933606196; do not rerun or dispatch another workflow
while it is active. On completion, preserve attempt-specific logs, job metadata
and compact artifacts before changing anything. Compare unchanged attempts. If
results differ, investigate the environment and retained diagnostics first.
If the runner disappears again without artifacts, improve failure-evidence
collection as a separately reproduced and validated change; do not guess at
scientific fixes from missing outputs. Require actual reduced execution,
convergence and reference evidence before making the corresponding claims.

The regular schedule remains daily at 01:00 Asia/Taipei; next occurrence after
this wake is 2026-09-21 01:00. No active local solver processes were started.
UI, guidebook, MCP/plugin and platform milestones remain in their existing order.

Record prepared: 2026-09-20T01:05:43+08:00 (Asia/Taipei).
