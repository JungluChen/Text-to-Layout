interface:
  display_name: "Text-to-GDS"
  short_description: "Generate, inspect, and simulate superconducting GDS layouts."
  default_prompt: "Use $text-to-gds to plan a 5 GHz LJPA, compile local GDS, run DRC/extraction, export a stack preview, generate a simulation plot, and summarize simulator status."
