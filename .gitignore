.venv/
.wsl-venv/
.tools/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
.agents/
skills-lock.json
.env

# textlayout plugin server output (any workspace variant)
workspace/textlayout*/

# CLI / CI scratch output directories
out/
ci_out/

# Non-reproducible benchmark generation metadata (wall-clock time, tool versions).
# The committed provenance uses a normalized timestamp; the real time lives here.
examples/**/.generation_meta.json

workspace/artifacts/*
!workspace/artifacts/.gitkeep
plugins/*/workspace/artifacts/*
!plugins/*/workspace/artifacts/.gitkeep

# External library source trees (cloned for reference, not project code)
external_repos/
quantum-eda-stack/
references/
workspace/catalog.py
plugins/*/examples/**/.generation_meta.json
external_references/
