interface:
  display_name: "Text-to-GDS"
  short_description: "Generate and validate superconducting GDS layouts."
  default_prompt: "Use $text-to-gds to compile a superconducting GDS layout, run DRC, and summarize the sidecar."

