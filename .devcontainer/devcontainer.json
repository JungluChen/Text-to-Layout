{
  "name": "TextLayout solver stack",
  "build": {
    "dockerfile": "../docker/solver-full.Dockerfile",
    "context": ".."
  },
  "workspaceFolder": "/work",
  "runArgs": ["--init"],
  "postCreateCommand": "textlayout doctor --strict-em"
}
