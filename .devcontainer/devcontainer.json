{
  "name": "textlayout-simulators",
  "build": {
    "dockerfile": "../docker/simulators.Dockerfile",
    "context": ".."
  },
  "postCreateCommand": "pip install uv && uv sync --dev && python scripts/bootstrap_simulators.py && make check-simulators",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "charliermarsh.ruff"
      ],
      "settings": {
        "terminal.integrated.defaultProfile.linux": "bash"
      }
    }
  }
}
